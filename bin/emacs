#!/bin/sh

# Starting emacs from a terminal gives you access to all the environment
# variables set in your initialization shell but without nice GUI features
# like mouse-selection, click-to-focus, nice fonts etc. This script gives
# us the best of both worlds

/Applications/Emacs.app/Contents/MacOS/Emacs "$@" &
